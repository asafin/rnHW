import React from 'react';
import { ConnectionError as ConnectionErrorComponent } from '../../components/connectionError/ConnectionError';

export const ConnectionError: React.FC = () => <ConnectionErrorComponent />;
